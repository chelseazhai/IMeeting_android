<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="20dp" >

    <RelativeLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content" >

        <TextView
            android:id="@+id/register_step3_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:text="@string/register_step3_textView_text"
            android:textAppearance="?android:attr/textAppearanceLarge"
            android:textColor="@color/dim_gray" />

        <TextView
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:layout_alignBaseline="@id/register_step3_textView"
            android:layout_toRightOf="@id/register_step3_textView"
            android:ellipsize="end"
            android:gravity="right"
            android:text="@string/register_step3_description"
            android:textColor="@color/light_dim_gray" />
    </RelativeLayout>

    <EditText
        android:id="@+id/pwd_editText"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:hint="@string/pwd_editText_hint"
        android:inputType="textPassword" >

        <requestFocus />
    </EditText>

    <EditText
        android:id="@+id/confirmationPwd_editText"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:hint="@string/confirmationPwd_editText_hint"
        android:inputType="textPassword" />

    <Button
        android:id="@+id/register_finish_btn"
        android:layout_width="fill_parent"
        android:layout_height="46dp"
        android:layout_marginTop="10dp"
        android:background="@drawable/accountsetting7register7newtalkinggroup_btn_bg"
        android:text="@string/register_finish_btn_title"
        android:textColor="@android:color/white" />

</LinearLayout>